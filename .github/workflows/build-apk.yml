name: Build Android APK

on:
  push:
    branches:
      - main
    tags:
      - 'v*' # Trigger otomatis rilis saat Anda push tag rilis
  workflow_dispatch: # Memungkinkan Anda untuk menjalankan workflow ini secara manual

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22' # Gunakan versi 22 yang belum deprecated
          cache: 'npm'
          
      - name: Install Dependencies
        # Lebih aman menggunakan install dibanding ci jika lockfile sering tidak sinkron
        run: npm install

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Capacitor
        run: |
          npm install @capacitor/core @capacitor/android
          npm install -D @capacitor/cli
          # Buat dummy index.html agar sync capacitor tidak error karena folder public kosong
          mkdir -p public
          echo '<!DOCTYPE html><html><head><meta http-equiv="refresh" content="0; url=https://web-music-new-update.vercel.app/" /></head><body></body></html>' > public/index.html
          npx cap init SannMusic com.sannmusic.app --web-dir public
          
      - name: Configure Capacitor as WebView
        run: |
          cat <<EOF > capacitor.config.json
          {
            "appId": "com.sannmusic.app",
            "appName": "SannMusic",
            "webDir": "public",
            "server": {
              "url": "https://web-music-new-update.vercel.app/",
              "cleartext": true
            }
          }
          EOF

      - name: Build Android App
        run: |
          npx cap add android || true
          npx cap sync android
          cd android
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SannMusic.apk
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Release to GitHub
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: android/app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
